{
  "name": "@data-foundry/query-model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The single canonical query layer shared by web, REST API and MCP: entity lookup, redirect-aware slugs, exact-first search, metadata-driven facets, fact/evidence reads, graph traversal and comparison.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run --root ."
  },
  "dependencies": {
    "@data-foundry/canonical-schema": "workspace:*",
    "@data-foundry/canonical-store": "workspace:*",
    "@data-foundry/provenance": "workspace:*",
    "zod": "^4.4.3"
  }
}
