{
  "name": "@data-foundry/usage-events",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The shape of a metering event, and the idempotent write both the producer and the consumer agree on.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "test": "vitest run --root .",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@data-foundry/canonical-store": "workspace:*"
  }
}
