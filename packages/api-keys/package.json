{
  "name": "@data-foundry/api-keys",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minting and verifying API credentials. Web Crypto only; no store, no HTTP, no query layer.",
  "exports": { ".": "./src/index.ts" },
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "test": "vitest run --root .",
    "typecheck": "tsc -p tsconfig.json"
  }
}
