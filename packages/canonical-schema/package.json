{
  "name": "@data-foundry/canonical-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for the Data Foundry canonical object model: entities, facts, evidence, relationships, confidence scores, rights classification and the ingestion job state machine.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "test": "vitest run --root ."
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
