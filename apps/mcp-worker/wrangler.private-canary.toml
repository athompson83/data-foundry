# Route-less target profile for the synthetic private canary. Copy this to the
# ignored wrangler.private-canary.production.toml and inject only account_id
# plus this Worker's role-specific Hyperdrive id. MCP host/origin policy is
# intentionally absent because the probe is a named service binding, not HTTP.
name = "data-foundry-private-canary-mcp-hvac"
main = "src/index.ts"
compatibility_date = "2026-08-23"
compatibility_flags = ["nodejs_compat"]
workers_dev = false
preview_urls = false

[observability]
enabled = true

[observability.logs]
invocation_logs = false

[vars]
DEPLOYMENT_ENVIRONMENT = "production"
PRIVATE_CANARY_MODE = "service-binding"

# [[hyperdrive]]
# binding = "HYPERDRIVE"
# id = "<mcp-role-hyperdrive-id>"

[[queues.producers]]
binding = "USAGE_EVENTS_QUEUE"
queue = "data-foundry-private-canary-usage-events"
