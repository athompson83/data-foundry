{
  "name": "@data-foundry/edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker serving the read-only REST surface. Composition root and transport adapter only; it owns no routing (ADR-0006).",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "files": [
    "src",
    "generated"
  ],
  "scripts": {
    "test": "vitest run --root .",
    "typecheck": "tsc -p tsconfig.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@data-foundry/access-auth": "workspace:*",
    "@data-foundry/api": "workspace:*",
    "@data-foundry/api-keys": "workspace:*",
    "@data-foundry/canonical-schema": "workspace:*",
    "@data-foundry/canonical-store": "workspace:*",
    "@data-foundry/query-model": "workspace:*",
    "@data-foundry/usage-events": "workspace:*",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
