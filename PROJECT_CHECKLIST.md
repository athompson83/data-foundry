# Project Checklist

## Project State

- Product: Data Foundry
- Lifecycle stage: MVP integrated / pre-deployment
- Current milestone: Integrate and deploy the first lawful, revenue-capable dataset. HVAC is the **reference vertical and first factory proof**, not the product identity and not a requirement to finish an industry before revenue — see the standing direction in [`AGENTS.md`](AGENTS.md) and [ADR-0012](docs/decisions/ADR-0012-machine-data-portfolio-and-capability-hostnames.md). Subsequent compatible datasets are expected to onboard primarily through configuration, mapping and validation rather than new application architecture or separate infrastructure.
- Current release target: one rights-approved HVAC equipment dataset delivered to machine clients through at least one verified paid machine-access path; six Cloudflare Workers, thirteen core ordinary/capability-canary artifacts and a separate synthetic ingestion profile; canonical Postgres and R2. The shortest supported paid path is the direct `API_PAID` API billed `DIRECT`, which needs no third-party enrollment; RapidAPI (`UA-004`) and pay per crawl (`UA-008`) are additional channels that do not gate the first paid request. See [the 2026-09-16 channel capability record](docs/evidence/machine-access-channel-capability-20260916.md). The remaining commercial decision on that path is reduced to four questions with proposed values in [the pricing and invoicing decision sheet](docs/owner-actions/direct-api-pricing-and-invoicing-decision.md).
- Protected-main state: live baseline rechecked 2026-09-18 at 55804842f5c5131640dd0435c7d203a66e95b63c (`main`, hosted CI run 35291689381 green). Exact candidate checks/review and protected integration remain separate from source/provider/revenue proof. Historical PR #26 and hosted September 2 evidence do not certify this expanded topology. The 2026-09-18 private-canary evidence certifies the route-less private profile only, whose seven deployed bundles byte-match the tracked manifests at this SHA.
- Last updated: 2026-09-18 (third session: ADR-0013 disposition, provider read-back tooling)
- Closeout state, 2026-09-17 (UA-002 line updated 2026-09-18) — the three tracks are separate and must not be read as one:
  - **Merged implementation.** PR #46 (`41dd2ec`) and PR #47 (`8a9542d`) are on `main`. The UA-002 credential step is an executed, tested script rather than prose, and the real-Postgres job publishes sanitized per-stage evidence.
  - **Hosted verification.** COMPLETE for the `UA-002` scope as of 2026-09-18, independently reconciled read-only in [the reconciliation record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md): hosted ledger **33 / 33 / 0** with every effective checksum matching release `2063ea8d72247a9b2643e1c690e37ab55ab14252`; the release's own `postCredentialVerificationSql` passes read-only (286/286 runtime grants; zero inventory, ownership, search-path, `SECURITY DEFINER`, default-ACL or `PUBLIC` drift); six LOGIN, NOINHERIT, membership-free runtime roles; six cache-disabled `verify-full` Hyperdrives on the uploaded Supabase Root 2021 CA; the five dedicated private-canary queues with the documented topology and the ordinary 14-day pair untouched; both R2 buckets; seven route-less private-canary Workers whose deployed bundles byte-match in-place builds of the tracked manifests at `55804842f5c5131640dd0435c7d203a66e95b63c`; private canary run `475e622a-a1bf-48a5-9d2b-52126281bff4` with six `READY` probes, exactly-once metering, an empty quarantine, the receipt retained in R2 and the fixture removed. Not independently readable with the available tooling and carried as Part 2 gaps: queue retention/depth and Worker route/workers.dev flags. **This is private-canary success, not production:** no ordinary Worker, route, hostname, Cron, real source, billing configuration or public DNS exists. The 2026-09-18 provider-staging record and the earlier hosted records are preserved as history.
  - **Commercial validation.** NOT VALIDATED. Zero independent buyer interviews; every threshold on the buyer scorecard reads 0, and all four HVAC sources remain explicitly SYNTHETIC. Capability-based canonical hostnames are standing direction only — no DNS, route or provider state is live. See [the first paid slice decision](docs/commercial-validation/first-paid-slice-decision-20260917.md).
- Detailed sources: [`README.md`](README.md), [`docs/owner-actions/cloudflare-deployment.md`](docs/owner-actions/cloudflare-deployment.md), the [2026-08-31 redacted provider reconciliation](docs/evidence/alpha-lab-provider-reconciliation-20260831.md), the [2026-09-02 hosted migration and grant evidence](docs/evidence/alpha-lab-hosted-migration-20260902.md), [`docs/source-onboarding.md`](docs/source-onboarding.md), and the vertical documents under [`verticals/hvac/`](verticals/hvac/)

Status vocabulary is limited to `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `DONE`, `DEFERRED`, and `SUPERSEDED`. Open-PR work is not treated as complete on `main`.

## Foundation

| ID | Phase | Description | Status | Acceptance criteria | Evidence | Dependencies / blocker | Completed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| FOUNDATION-001 | Foundation | Canonical schema, migrations, provenance, query model, acquisition, extraction, normalization, resolution, and storage architecture | DONE | Synthetic HVAC sources run end to end with evidence-backed canonical output and deterministic checks | `tests/e2e/factory-proof.test.ts`; `README.md`; PR #22 merged as `9c917c0` with migrations through `0026`; explicit source-record revision states and stream membership; accepted complete-snapshot watermarks/retirements; `source-record-evidence@3`; append-only alias claims with authority epochs, exact claim/evidence linkage, and no legacy authority backfill; bounded indexed entity/fact authorization; current entity/relationship support gates; executor-affine rollback controls; real-Postgres reconciliation/concurrency regressions pass | None | 2026-08-30 |
| FOUNDATION-002 | Foundation | REST, MCP, and export surfaces use the canonical query layer | DONE | Surface-boundary and parity tests pass on `main` | `apps/api`; `apps/mcp`; `services/export-builder`; `tests/contract/surface-parity.test.ts` | None | 2026-08-23 |
| FOUNDATION-003 | Foundation | Cloudflare Worker composition root | DONE | Worker composes API/query/database runtime without an in-memory production fallback | `apps/edge`; ADR-0006; PR #11 merged in `6db77e0` ancestry | None | 2026-08-23 |
| FOUNDATION-004 | Foundation | API tenancy schema | DONE | Tenant, credential, and usage foundations exist with migration verification | `db/migrations/0011_api_tenancy.sql`; PR #12 merged as `6db77e0` | None | 2026-08-23 |
| FOUNDATION-005 | Foundation | Repository governance and session handoff standard | DONE | Canonical control standard, executive checklist, and current handoff are merged into `main` and referenced by `AGENTS.md` | PR #18 merged; `APP_PROJECT_CONTROL_STANDARD.md`; `AGENTS.md`; `PROJECT_CHECKLIST.md`; `PROGRESS.md` | None | 2026-08-26 |
| FOUNDATION-006 | Foundation | Canonical Cloudflare and Alpha Lab database topology | IN_PROGRESS | Account, zone/routes, Hyperdrive, production Postgres, R2, Queue/DLQ, Cron, and bindings are configured and evidenced | **Hosted and independently read back 2026-09-18** ([reconciliation record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md)): the hosted `data_foundry` schema is at 33/33 migrations with the release's own verification passing read-only (286/286 grants, zero drift); six LOGIN, NOINHERIT, membership-free runtime roles; six cache-disabled `verify-full` Hyperdrives bound to the uploaded Supabase Root 2021 CA, one per role, on the IPv4 pooler; both R2 buckets; the five dedicated private-canary queues with the documented producer/consumer/DLQ topology and the ordinary 14-day usage pair untouched; seven route-less private-canary Workers whose deployed bundles byte-match in-place builds of the tracked manifests at `55804842f5c5131640dd0435c7d203a66e95b63c`. `aroqon.com` is active/full; the six tracked Wrangler templates and `cloudflare:artifacts:check` validate the deployment shape. Preserved as superseded history: the [2026-08-31 provider reconciliation](docs/evidence/alpha-lab-provider-reconciliation-20260831.md); the [2026-09-02 hosted migration evidence](docs/evidence/alpha-lab-hosted-migration-20260902.md), which truthfully recorded 26 applied migrations, staged `NOLOGIN` roles, the then-current 200-grant matrix and 57 function-search-path warnings (all since closed by `0027`–`0033` and the grant upgrade); the authoritative 2026-09-02T14:46Z Cloudflare refresh, which observed the standard usage model and exactly the ordinary 14-day Queue/DLQ pair with zero Data Foundry Workers, Hyperdrives, R2 buckets or routes; and the [2026-09-18 provider-staging record](docs/evidence/ua002-provider-staging-executed-20260918.md). | Still not hosted: the six ordinary production Workers, their routes/custom hostnames (owner cutover `UA-005`), Cron triggers, the ordinary raw-artifact and ingestion Queue bindings, and public DNS. Queue retention/depth and Worker route/workers.dev state were not readable by the reconciliation session; `pnpm cloudflare:readback:check` (added 2026-09-18, [ADR-0013](docs/decisions/ADR-0013-private-canary-resource-disposition.md)) reads them from the REST API but has not yet been run against the account, so they remain unattested. | — |
| FOUNDATION-007 | Foundation | GitHub repository protection and security configuration | DONE | Active `main` ruleset, strict required checks, vulnerability reporting, dependency security updates, secret scanning, and push protection are verified | Ruleset `21855694`; `main` protected; `docs/owner-actions/repository-governance.md` | Obsolete Vercel App access is tracked separately under `PROD-005` and `UA-003` | 2026-08-30 |
| FOUNDATION-008 | Foundation | Local Windows test portability | DONE | Architecture-boundary tests resolve file URLs correctly when the checkout path contains spaces | `packages/extraction/test/boundary.test.ts` and `packages/normalization/test/contract.test.ts` use `fileURLToPath`; focused suites pass 10/10 and repeated full local suite passed 3,246/3,246 on 2026-09-03. On 2026-09-18 the full suite on Windows reports 3,556/3,583: the 27 failures are confined to three POSIX-only tooling tests added by PR #47 (`ua002-migration-pgpassfile`, `ua002-credential-placement-doc`, `ci-postgres-readiness`: `0600`/`0755` file modes, Git Bash `/tmp` remapping, backslash separators) plus two vitest worker-timeout errors, while hosted Linux CI on the same SHA is green (run 35291689381). | Windows portability of those three POSIX helper tests is open local debt, not a release blocker | 2026-09-03 |

## MVP

| ID | Phase | Description | Status | Acceptance criteria | Evidence | Dependencies / blocker | Completed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| MVP-001 | MVP | Rights-grant model supports different lawful uses and access tiers | DONE | Accepted rights architecture and implementation are integrated on protected `main` without implying source clearance | Accepted ADR-0010; `packages/rights-engine`; PR #22 merged alias-evidence migration 0025 and authorization-streaming migration 0026 with 0014/0016/0019; exact effective surface matrices; provenance AND; exact historical fact selection with current publication rights; source alias claims bind exact immutable evidence before resolution/search; current identity/relationship support remains separate from legal permission; recursive historical-contributor and bulk-export controls; no migration-created `ALLOW`; canonical `sources:readiness --as-of` with named live DB or validated qualified snapshot | Source-specific review remains independently tracked by `MVP-002` and `UA-001` | 2026-08-30 |
| MVP-002 | MVP | First real HVAC source approved and ingested | BLOCKED | At least one real source passes rights review, acquisition, extraction, normalization, evidence, quality, and publication gates | `verticals/hvac/RIGHTS.md`; source review packets under `docs/sources/`; rights-aware `sources:readiness` output identifies exact missing operation/channel cells while keeping the proposed ENERGY STAR source deferred | Rights approval and real-source acquisition are incomplete | — |
| MVP-003 | MVP | Scheduled refresh policy and runner | DONE | Cadence policy is integrated, wired to Cron, rights-gated at every acquisition boundary, stores immutable R2 evidence, and records durable run/failure state | `apps/acquisition-worker`; migrations 0017/0019/0020; versioned four-checkpoint receipts; fenced same-row retry/crash recovery; non-owner claim observations and diagnostic reads omit the fencing token; post-transport revocation negative; direct/provider size, page, cursor, diagnostic, cumulative-artifact, and no-partial-write controls; acquisition compiler, PostgreSQL, topology, and artifact checks | Live Cron/R2/Hyperdrive proof is independently tracked by `BETA-002` and `FOUNDATION-006` | 2026-08-30 |
| MVP-004 | MVP | Privacy-safe API usage schema and vertical accounting | DONE | Route closed vocabulary, vertical-total accounting, authority corrections, access/billing channel isolation, and migrations are integrated and verified | migrations 0012/0015/0018; `packages/usage-events`; route-key privacy, vertical attribution, and direct-invoice exclusion tests | None | 2026-08-30 |
| MVP-005 | MVP | API authentication and asynchronous usage metering | IN_PROGRESS | Scoped API keys and queue-backed idempotent usage persistence are merged and verified on deployed Cloudflare infrastructure | `apps/edge`; `apps/usage-consumer`; ADR-0009; durable queue-acceptance and idempotent-consumer tests; fail-closed one-vertical `credentials:provision`; 2026-08-31 regression coverage proves fresh, private-schema Hyperdrive clients close per invocation and cannot leak a shared Alpha Lab `search_path`. **Live 2026-09-18:** the route-less private canary queued each synthetic metering event twice from the reduced edge and MCP targets through the dedicated `data-foundry-private-canary-usage-events` Queue, the reduced usage consumer persisted them through its `df_usage` Hyperdrive, and `private-canary-fixture.ts verify` reported both present exactly once; the fixture was then removed and this session confirmed no residue ([record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md)). | Bearer-key HTTP authentication has not been exercised on a deployed Worker: the canary probes are service-binding RPC with fixture keys, and the ordinary edge/MCP Workers are not deployed. That is Part 2 verification work, not a hosting gap. | — |
| MVP-006 | MVP | Public multi-industry web surface | IN_PROGRESS | Human pages, crawl controls, quality gates, evidence links, sitemaps, and web runtime are integrated and deployed | `apps/web`; ADR-0011; generated runtime and dispatch/SEO tests; production-enforced `PUBLIC_CACHE_MODE=no-store`, cache-isolated composition, shared request-wide sitemap scan budget, atomic capacity refusal, canonical shard parsing, and fixed-worker authorization controls | Cloudflare deployment only; public caching requires a later rights-lifetime invalidation implementation, and sitemap routes require a live provider rate-limit rule | — |
| MVP-007 | MVP | RapidAPI/public API distribution and billing path | IN_PROGRESS | Canonical API adapter, marketplace-origin authentication, OpenAPI contract, billing-channel isolation, product offering, and marketplace integration are verified end to end | `apps/edge/test/rapidapi.test.ts`; generated public marketplace projection `openapi/data-foundry-hvac-rapidapi-v1.openapi.json` with canonical route/schema parity and no origin bearer; direct-invoice exclusion test; direct-to-Wrangler `RAPIDAPI_API_KEY` provisioning through the account-bound, pre-existing edge deployment | RapidAPI enrollment, proxy secret, plans, payout, live route, and real subscriber proof only; repository implementation is merged | — |
| MVP-008 | MVP | Security and privacy baseline | IN_PROGRESS | Authn/authz, rights, secret handling, data minimization, retention/erasure, dependency review, and abuse controls are verified for the release candidate | `SECURITY.md`; `DATA_RIGHTS.md`; privacy tests; non-owner acquisition-capability redaction; bounded sitemap work; `unpdf@1.8.1` removes the vulnerable `node-tar` chain; `esbuild` is upgraded to the non-vulnerable line; parsed-lockfile policy and a full moderate-or-higher dependency audit report no known vulnerabilities; protected-branch security controls; historical sealed review only (the current PR30 review is incomplete after a blocked delegated validation); `docs/owner-actions/data-retention-and-erasure.md`; merged PR #26's shared runtime/migration-role matrix rejects neighboring PostgreSQL privileges, unsafe durable/session state, object ownership, cross-database reachability, and quoted shared-`public` migration targets without normalizing provider state | Provider rate limiting/abuse protection and production evidence remain incomplete; exact-SHA security review remains a per-release gate | — |
| MVP-009 | MVP | Accessibility, SEO, analytics, and observability baseline | IN_PROGRESS | Relevant public pages meet accessibility/SEO criteria; errors, usage, and critical runtime health are observable without leaking sensitive data | `apps/web`; generated web runtime; SEO, sitemap, structured-data, rights, accessibility, and dispatch tests; logging in current services | Web deployment, analytics, and production monitoring evidence only | — |
| MVP-010 | MVP | Deployable MCP / agent-access surface | DONE | The six generic canonical-query tools run through MCP 2026-07-28 Streamable HTTP with exact one-vertical custom-bearer `MCP/NONE` auth, live rights freshness, privacy-safe Queue analytics, generated runtime parity, and no local-only runtime in the Cloudflare bundle | `apps/mcp`; `apps/mcp-worker`; `packages/access-auth`; migration 0018; `verticals/hvac/mcp.yaml`; MCP Worker and Cloudflare artifact tests | Live hostname, Hyperdrive/Queue, exact real-source grants, packaging, and client proof are deployment/commercial gates, not repository implementation | 2026-08-30 |

## Beta

| ID | Phase | Description | Status | Acceptance criteria | Evidence | Dependencies / blocker | Completed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BETA-001 | Beta | Browser, REST, marketplace, and MCP end-to-end coverage against Preview | NOT_STARTED | Real search/entity/docs flows, API/MCP auth, wrong-channel and rights negatives, error states, mobile/tablet layouts, console, and network behavior pass in isolated deployment | No deployed Preview recorded | MVP surfaces and canonical infrastructure | — |
| BETA-002 | Beta | Live Cloudflare Queue and database integration proof | DONE | At-least-once delivery, duplicates, retries, DLQ, and persistence are verified against real isolated provider resources | Private canary run `475e622a-a1bf-48a5-9d2b-52126281bff4` (issued 2026-09-18T15:09:24.235Z, completed 15:11:55.858Z) on the dedicated, isolated canary resources: the control envelope reached the harness only through `data-foundry-private-canary-events` → three consumer retries → `data-foundry-private-canary-dlq` (that DLQ is the harness's sole input, so the receipt itself proves retry and dead-letter delivery); the edge and MCP targets each sent their metering event twice and the database held each exactly once (idempotent at-least-once persistence); all six probes `READY`; quarantine left without a consumer or producer and reported empty; receipt `runs/475e622a-a1bf-48a5-9d2b-52126281bff4/20260918150924235.json` retrieved from `data-foundry-private-canary-receipts` (sha256 `81435077ca99d64868f031d733bb849b3dc3193fa7cc3f3b255925c9c91269e4`); fixture removed. Queue IDs, Worker version IDs and bundle digests are in the [2026-09-18 reconciliation record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md). [Edge producer acceptance](apps/edge/test/index.test.ts), [MCP producer acceptance](apps/mcp-worker/test/worker.test.ts) and [usage-consumer retry/idempotency/DLQ behavior](apps/usage-consumer/test/consumer.test.ts) remain the local contracts. | Proven on the isolated private-canary topology only; the ordinary `data-foundry-usage-events` pair has no deployed producer or consumer yet (`PROD-002`). Queue retention/depth were not independently readable by the reconciliation session; the read-back command now exists (`pnpm cloudflare:readback:check --phase private-canary`, 23 unit tests against captured API shapes) and awaits one execution with a read-only token. The canary resources are retained under [ADR-0013](docs/decisions/ADR-0013-private-canary-resource-disposition.md). | 2026-09-18 |
| BETA-003 | Beta | Operational runbooks and recovery | IN_PROGRESS | Deploy, migration, rollback, backup/restore, incident, data-erasure, and key-revocation paths are executable and tested | Existing owner-action runbooks cover portions. 2026-09-18: [ADR-0013](docs/decisions/ADR-0013-private-canary-resource-disposition.md) fixes the private-canary topology as standing pre-cutover validation infrastructure and redefines canary-identity deletion as the rollback path; `pnpm cloudflare:readback:check` gives the runbook a provider read-back for queues, routes, domains and subdomain flags (unit-tested, not yet executed against the account). | Canonical production topology (ordinary six Workers, route-less first) and the hosted backup/restore and rollback exercises | — |
| BETA-004 | Beta | Critical findings and stale branches closed | DONE | Relevant open PRs/findings are resolved; merged or obsolete branches contain no unique work and are cleaned safely | PR #19 merged the reconciled source, usage, rights, web, RapidAPI, acquisition/readiness, and MCP slices; PRs #13–#17 were path/patch/ancestry/behavior checked and closed as superseded, with non-ancestor branches retained deliberately as recovery points; PR #22 exact head `501b33d` passed 183 files/2,926 tests, hosted run `33352124668`, exact-head reviews, and sealed security scan `24b34cd2-2f8d-40ae-bfd2-f4460daa419f` before merging as `9c917c0`; PR #26 exact head `8a43b7f7600fef10c1b26f0281a4c087f8610373` passed both required checks and automated reviews with zero unresolved threads before merging normally as `02e90d70d0000d21c7f9b070b4e1b2e1d5dd7493` | None | 2026-09-02 |

## Production

| ID | Phase | Description | Status | Acceptance criteria | Evidence | Dependencies / blocker | Completed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PROD-001 | Production | Production database, migrations, backup, and recovery | IN_PROGRESS | Exact production target, migration set, backup, restoration and recovery point evidenced | **Migration set hosted and verified 2026-09-18:** `data_foundry.schema_migrations` on `fgxinxaqkwoqyywdgobs` holds `0001`–`0033` (the seven pending rows applied 2026-09-18T14:06:06Z–14:06:12Z through the direct-TLS operator), every effective checksum matches release `2063ea8d72247a9b2643e1c690e37ab55ab14252`, and the release's `postCredentialVerificationSql` checks pass read-only: 55 relations and 59 functions owned by `df_migration` with `search_path=data_foundry, pg_catalog, extensions`, zero `SECURITY DEFINER`, 286/286 runtime grants, no `PUBLIC`/`anon`/`authenticated`/`service_role` private ACL, seven canonical durable role settings ([record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md)). Historical: the 26-migration/200-grant state of 2026-09-02; local native PostgreSQL 15 and 16 proofs; migrations 0032–0033 local regression coverage. | Backup, isolated restore, and rollback exercises remain unverified on the hosted target (`BETA-003`, `REV-006`). | — |
| PROD-002 | Production | Production Cloudflare deployment | IN_PROGRESS | Reviewed SHA/deployment IDs and customer workflows verified in the canonical account | **Private canary deployed and passed 2026-09-18:** seven route-less private-canary Workers exist in the canonical account with version IDs read back and bundles byte-matching in-place builds of the tracked manifests at `55804842f5c5131640dd0435c7d203a66e95b63c`; each reduced target binds only its role-specific Hyperdrive, its documented private Queue producer/consumer, `DEPLOYMENT_ENVIRONMENT` and `PRIVATE_CANARY_MODE`; the harness binds only the six service entrypoints and the receipt bucket ([record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md)). The September 2 "no Workers/Hyperdrives/R2" observation is superseded history. Thirteen core bundles and a separate synthetic ingestion profile still build from the tracked templates. | **Not a production deployment.** The six ordinary Workers, Cron, ordinary Queue/R2 bindings, routes and hostnames are not deployed; `UA-005` public cutover, rollback proof and customer-workflow verification remain. Worker routes, custom domains and workers.dev/preview flags were not readable by the reconciliation session; `pnpm cloudflare:readback:check` now reads them (and, with `--phase ordinary-route-less`, verifies the six ordinary Workers once deployed without routes) but has not yet been run against the account. | — |
| PROD-003 | Production | Production security, privacy, legal, and billing readiness | BLOCKED | Security review, retention/erasure, rights approvals, terms/privacy, abuse protection, billing, and settlement configuration are complete | `SECURITY.md`, rights and owner-action docs identify gaps | Legal/business decisions and technical MVP work | — |
| PROD-004 | Production | Monitoring, alerts, analytics and operations | IN_PROGRESS | Real failure/recovery alerts, independent outage/spend monitoring and tested operator recovery | Durable closed-code incident/notification state, restricted email binding disabled by default, operator status/replay/backfill/pause/revocation/closure and immutable audit implemented; local failure/recovery and unknown-send controls pass. | Verified email (UA-007), provider monitoring, actual inbox delivery and cost evidence; no approved retention schedule | — |
| PROD-005 | Production | Stale code, infrastructure, issue, and branch cleanup | BLOCKED | Obsolete paths and redundant infrastructure are removed after proof; completed findings/issues are closed; merged branches are deleted safely | PR #19 is merged; PRs #13–#17 are closed as superseded after unique-work inspection; repository hooks are empty. Vercel project `prj_lU0xruXu9hA6A0UpZYeUZpxW3712` has disconnected Git and serves no Data Foundry application at the configured host. | The sudo/passkey Vercel-App selection check remains owner-only; do not delete a canonical project or detach a production hostname until the Cloudflare canary has proved its replacement. | — |
| PROD-006 | Production | Product Owner declares Data Foundry shipped and live | NOT_STARTED | Product Owner explicitly changes pre-shipping authority after successful production verification | Not declared | PROD-001–PROD-005 | — |

## Revenue delivery workstream — 2026-09-08

The accepted plan is [recorded here](docs/superpowers/plans/2026-09-08-recurring-revenue-platform.md). These are end-to-end milestones; local engineering completion does not mark a live milestone DONE. Original checkout changes were preserved in isolation. The README-only PR29 presentation is incorporated, with all previous operational prose preserved under docs/reference/platform-reference-20260903.md.

| ID | Phase | Description | Status | Acceptance criteria | Evidence | Dependencies / blocker | Completed |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REV-001 | A | Source, equipment slice and buyer validation | BLOCKED | Human-reviewed rights/coverage, golden data and three prospective integrations | Fresh source assessment, data dictionary, offer/pricing hypotheses and unsent outreach packet | UA-001; no real source or buyer validation; outreach needs explicit sending authorization | — |
| REV-002 | B | Automatic acquisition-to-publication | IN_PROGRESS | Two scheduled real-source cycles publish canonical changes without manual commands | Transactional outbox, opaque queue, independent dispatcher, leased/fenced JSON/CSV ingestion, verified R2 manifests, atomic promotion and 304 handling; a NOT_MODIFIED run can select only a same-scope FETCHED run whose completion and freshness precede its claim; real-role local publication with 61 facts/evidence and nine negative controls | Real source + canonical provider bindings; qualified limits 16 artifacts/4 MiB/1,000 records, one target per source; 10,000 affected entity/property pairs per delivery; larger/partitioned feeds require a completion-barrier implementation | — |
| REV-003 | C | Freshness, operations and recovery | IN_PROGRESS | Accurate clocks, observed failure/recovery, real alerts and approved retention/erasure | Optional bounded refresh interval; ingestion/operator/alerts migrations 0029–0031; ten operator tests; closed telemetry; alert acceptance/unknown-outcome tests | UA-007; independent provider monitoring, real email, restore, rollback, retention implementation after policy approval | — |
| REV-004 | D | Reusable vertical foundation | IN_PROGRESS | Second vertical works by config/adapters and survives protected integration | Shared compiled identifier rules, synthetic laboratory fields/relationships/filters, source-scope/collision/Unicode tests; migration0032 projects every current source claim without raw-history grants, extended industry template and adapter contracts | Exact candidate integration/review; no additional real vertical advertised | — |
| REV-005 | E | Marketplace customer journey | IN_PROGRESS | External subscriber pays, gets useful authorized results, integrates and can cancel/get support | /hvac routes and legacy redirects, coverage and proposed prices, listing/policy/contact gates, filters/pagination, TS/Python examples, shared selected-fact evidence, local mobile/browser validation | UA-001/004/007; actual listing, quota/cancellation/reconciliation and supported MCP client proof | — |
| REV-006 | F | Six-role deployment and recovery proof | IN_PROGRESS | Exact current-SHA provider canaries, resource isolation, restoration and rollback pass before public cutover | **Canaries and isolation passed 2026-09-18:** six role-specific Hyperdrives (cache disabled, `verify-full`, uploaded CA) over six LOGIN, membership-free `df_*` roles holding exactly the 286 expected private grants and nothing outside current-database `CONNECT` and `extensions` `USAGE`; seven route-less private-canary Workers bound to the current app sources; run `475e622a-a1bf-48a5-9d2b-52126281bff4` all six probes `READY`, metering exactly once, quarantine empty ([record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md)). The frozen 199-grant upgrade executed; migration inventory 33. | Restoration and rollback exercises are not yet performed on the hosted target; then `UA-005`. No deployment or DNS mutation in the reconciliation session. | — |
| REV-007 | G | Additional paid channels | DEFERRED | Hosted Parquet/exports, direct Stripe subscriptions, entitled MCP and more lawful verticals | Existing export/identity foundations; follow-on scope in accepted plan | First paid marketplace release and measured unit economics | — |

## Required User Actions

| ID | Action | Why owner-only | Blocks | Safe work meanwhile | Recommended choice |
| --- | --- | --- | --- | --- | --- |
| UA-001 | Name a human rights reviewer and approve one equipment slice/source with explicit acquisition, normalization, RapidAPI and refresh permissions; review web/index/MCP/bulk independently | Engineering cannot infer legal permissions or sign source terms | Real-source publication and paid dataset; platform engineering continues | **No candidate is selectable today — [first-source decision sheet](docs/owner-actions/ua-001-first-source-decision-20260916.md), re-compared on current evidence 2026-09-16.** **DOE CCMS:** not selectable on *access*, not rights — its edge returns 403 to an honest agent for `robots.txt` itself, so crawl permission cannot be established without impersonation. No CCMS dataset appears in data.gov's advertised sitemap (all 112 shards, 559,455 URLs); because the catalogue API and search are broken and the site appears mid-rebuild, that is absence from the sitemap rather than proof no record exists. **ENERGY STAR:** needs **two** approvals, not one — the rights question (the EPA licence is scoped to data *produced by* EPA while brand, model and rating fields are partner-/CB-submitted) **and** an acquisition-method authorisation, which review packet §2 records as *proposed, not authorised* with terms, rate limits and redistribution constraints `[UNVERIFIED]`. Its licence pointer was already `[VERIFIED]` in that packet; nothing measured on 2026-09-16 narrowed the legal question. **AU Energy Rating:** genuine CC-BY 3.0 AU licence, but `data.gov.au/robots.txt` is `User-agent: * / Disallow: /` for the entire host under `respect_robots: true`, so there is no approved automated acquisition path without written DCCEEW permission; its AS/NZS metrics are different test procedures from the dictionary's US DOE metrics and do not convert. Refresh cadence is **unknown** for both ENERGY STAR and AU — dated observations establish no frequency. **A paid direct API needs at least EIGHT rights cells, not seven:** `ACQUIRE`, `STORE` and `CACHE` before transport per `docs/source-onboarding.md` Stage 2, `NORMALIZE` and `DERIVE` in the pipeline, and `SERVE_API_ACCESS`, `SELL_API_ACCESS`, `REDISTRIBUTE_NORMALIZED` on `DIRECT_CUSTOMER_API`. Omitting `CACHE` fails the first fetch closed | **Owner direction 2026-09-16: proceed with counsel review of ENERGY STAR as the preferred US first-source candidate.** Return the [counsel packet](docs/owner-actions/energy-star-counsel-packet.md) answer — one narrow question on whether the EPA licence reaches partner- and certification-body-submitted values; trademark/mark use is tracked separately and Data Foundry will not use the mark. Separately authorise the acquisition method, which counsel's answer does **not** cover. Record **all eight** rights cells, not seven. [AU Energy Rating](docs/owner-actions/au-energy-rating-fallback-slice.md) stays qualified as the fallback and is not to be built unless ENERGY STAR is rejected, materially delayed, or demand validation supports AU/NZ first. [The DOE inquiry](docs/owner-actions/doe-send-authorization.md) needs **one send authorization** plus a sender identity and a contact route — the CCMS contact page 403s this environment, so the route cannot be confirmed from here |
| UA-002 | **COMPLETED 2026-09-18.** The controlled `df_migration` login applied `0027`–`0033` and the exact-baseline grant upgrade through the direct-TLS operator; six distinct runtime credentials were activated and six cache-disabled `verify-full` Hyperdrives created; the route-less private canary then passed. Independently reconciled read-only against the provider and the hosted database on 2026-09-18 — see [the reconciliation record](docs/evidence/ua002-hosted-execution-reconciliation-20260918.md); the six direct runtime-role probes were reported by the executing session and are corroborated (Hyperdrive creation and every target's own session/current-role assertion before `READY`) rather than re-run. Historical action text follows. After containment, activate the controlled migration credential; apply pending reviewed migrations and the exact-baseline grant upgrade; securely activate six distinct runtime credentials and six cache-disabled Hyperdrives | Credentials require owner-controlled secure entry; shared-database privileges cannot be guessed or normalized | Hosted migration, binding and provider canary proof | UA-006 is cleared. Cloudflare CLI read-only inventory and the current-main local migration, role, topology and artifact gates passed; direct-TLS and runtime credential interfaces remain unavailable. Re-measured 2026-09-16: hosted ledger is `0001`-`0026`, `df_ingestion` absent, all roles `NOLOGIN`; the release carries `0033` and 286 expected grants. Direct TLS was unreachable on three counts (no credential in the environment, IPv6-only origin with no IPv6 stack, IPv4 pooler timing out behind an HTTPS-only proxy), and the runbook directs stopping rather than substituting a connector. [Channel and interface evidence](docs/evidence/machine-access-channel-capability-20260916.md). The owner-approved connector exception could not be executed: on 2026-09-16 the management connector authenticates as `supabase_read_only_user` with `default_transaction_read_only = on` and no `df_migration` membership, so the required `SET ROLE` probe failed closed with `42501` before any mutation. The read-only control was not worked around. The hosted baseline was verified undrifted first, and all seven pending packets plus the grant upgrade are checksum-verified against the release Git objects. [Write-path evidence](docs/evidence/ua002-connector-write-path-20260916.md); [handover](docs/owner-actions/ua-002-hosted-migration-handover.md). | **The one missing capability is a host with PostgreSQL egress to the project origin, running release `2063ea8`, holding the `df_migration` password in libpq's secure path — any ordinary developer machine qualifies.** Established by measurement on 2026-09-16: only one agent environment exists and it is the restricted one, the origin is IPv6-only against a container with no IPv6 stack, raw TCP is refused at the sandbox policy layer, the connector is still read-only, and binding the migration principal to a Worker is forbidden. The hosted baseline is undrifted, the 26 applied ledger rows are byte-identical to the release, and the documented sequence reproduces 33/26/7 with all ten checksums from the real hosted ledger; the operator stops at the credential and nowhere else. The IPv4 Supavisor pooler must be used in session mode (5432), not transaction mode (6543), which would not preserve the session state the runner asserts. [Execution-environment evidence](docs/evidence/ua002-execution-environment-20260916.md). **Superseded 2026-09-18: all three provider-path prerequisites are CLEARED on the hosted database.** They were (and none could be done by `df_migration`, which is `NOCREATEROLE` and was `NOLOGIN`): activate the migration login; repair durable role settings on all six roles (12 measured violations — every role carried a forbidden all-databases `search_path` row and none carried the required current-database row, which would have failed the grant upgrade *after* the migrations applied); and stage `df_ingestion`. The reviewed `ua-002-provider-staging.sql` executed them unchanged in one privileged session through the Supabase MCP `apply_migration` tool — a **correction** to the earlier record here, which concluded the connector was read-only from a measurement of `execute_sql` alone; `execute_sql` is still read-only (`ERROR 25006` on `ALTER ROLE`, nothing mutated), `apply_migration` is privileged and had never been tested. It is the same provider path the `20260901001729` role skeleton came through, not Option 2's whole-project write mode, which remains un-taken. Verified after: 0 all-databases settings, 0 privileged-or-inheriting `df_*` roles, 7 roles present, `df_ingestion` holding CONNECT and `extensions` USAGE and deliberately **not** `data_foundry` USAGE (that arrives with the grant upgrade), and no new advisor finding. The pending migrations were **not** applied through the same tool: `migrate.ts` asserts `session_user` and `current_user` both equal `df_migration`, which a privileged provider connection cannot satisfy, and hand-applying would bypass the preflight and leave the ledger unwritten. Then run the prepared sequence over direct TLS connecting **as** `df_migration` (the runner asserts `session_user` and `current_user` both equal it and rejects `SET ROLE` or a broader credential), and require upgradeFrom0028Sql/verificationSql before runtime LOGIN, then postCredentialVerificationSql and all six direct runtime probes before Hyperdrives — six of them, one per database-backed Worker. The operator is merged at `2063ea8d72247a9b2643e1c690e37ab55ab14252` and the handover, merged at `5e263fc9326962de4e009b9047e5af1a04053df5`, is bound to it with the exact command; all ten documented checksums were regenerated at `5e263fc` after that merge and reproduce unchanged. `pnpm ua002:operator -- --packet <manifest>` performs the whole readiness check read-only in fifteen probes and reports every blocker in one pass; `--apply` then runs migrations, the grant upgrade and verification in order, executing a grant payload rebuilt from the release rather than the manifest's. Checks deliberately excluded because the pending migrations themselves repair them — function search paths (`0027`), the `SECURITY DEFINER` functions `0029`/`0033` replace, and the 200→199 acquisition ACL reshape (`0027`) — are recorded as residuals with the measurements behind each |
| UA-003 | Inspect Vercel GitHub App installation `122728140` after GitHub sudo/passkey confirmation; remove only `athompson83/data-foundry` if it still has access and record the result | The independent sudo/passkey ceremony and App repository selection cannot be completed by the current automation credential | Obsolete-Vercel hygiene and `PROD-005`; not protected merge or Cloudflare deployment | Exact-SHA verification, protected-main integration, and Cloudflare preparation are complete; Vercel verification remains independent | Preserve other Vercel-managed repositories; keep `vercel.json` until Data Foundry access is proven absent |
| UA-004 | Complete RapidAPI account enrollment, marketplace agreements, payout and plan configuration, set the protected proxy proof/marketplace credential, and run one live test-subscriber request | Third-party enrollment, legal acceptance, payment configuration, dashboard approval, and secret-bearing live proof require the account owner | RapidAPI channel only; direct API, MCP, public web, bulk, and synthetic Cloudflare proof remain independent | Thin canonical adapter, isolated marketplace principal, rights enforcement, OpenAPI generation/drift check, analytics metering, and direct-invoice exclusion are merged and tested | Enroll one HVAC listing only after the exact real-source `RAPIDAPI` rights bundle exists; verify one request and no double billing |
| UA-005 | After the route-less, service-bound private canary passes its database, queue, R2, API, web, and MCP probes, separately authorize any public canary or `data.aroqon.com` cutover | Creating a public hostname or moving production traffic is user-visible and needs separate confirmation at the point of action | Public canary and final Aroqon production routing only | Keep public DNS/routes unchanged while proving the replacement through service bindings with no public hostname | Authorize public exposure only after the exact protected-main SHA, all required CI, and route-less real-provider checks are green; retain a documented rollback route |
| UA-006 | COMPLETED 2026-09-08. The Product Owner cleared the provider-side containment gate through normal security controls; no sensitive item, value, identifier, or browser state is recorded here. | The disposition is owner-controlled and deliberately sanitized. | Cleared for provider activation work under UA-002. | Repository and provider preparation may proceed without reopening, recovering, displaying, or rotating unrelated sensitive state. | Retain only this sanitized clearance record. The prior `unable_to_target_from_sanitized_evidence` fallback did not apply because the owner returned an affirmative disposition. |
| UA-007 | Provide verified operations/support contact and approve the concrete customer policies and retention schedule | Legal identity, rights appendix, retention basis/duration and staffed support commitments belong to the operator | Commercial activation, actual alerts and scheduled erasure; other engineering continues | Reviewable draft at docs/product/customer-policy-review-draft.md; closure and key revocation work without making an erasure claim | Approve explicit policy versions/URLs and verified email; engineering will configure and test the approved result |
| UA-008 | Request admission to the Cloudflare Pay Per Crawl closed beta (signup form, or an Enterprise account executive), and record the decision | Beta admission is a provider-controlled enrollment outside available tooling; there is no self-serve path | The crawler-revenue channel only. It blocks no other channel, and the direct paid API does not depend on it | Verified 2026-09-16 as closed beta with Web Bot Auth identity, `crawler-price`/`crawler-exact-price`/`crawler-charged` headers and Cloudflare as Merchant of Record; WAF and Bot Management block rules take precedence over charging. Meanwhile the commercial plan and evidence record were corrected to that reality | Request admission if crawler revenue is wanted; otherwise record a deliberate decision not to pursue it, so the channel stops being carried as indefinitely pending |

## Deferred

| Item | Status | Reason | Revisit trigger |
| --- | --- | --- | --- |
| Automated mutation testing platform | DEFERRED | Current work uses targeted manual mutations; introducing a new platform is not required for the immediate release path | Mutation coverage becomes unreliable or regression volume justifies automation |
| Dedicated graph database or search cluster | DEFERRED | `AGENTS.md` scope control rejects these without measured need | Postgres/query evidence proves the current architecture insufficient |
| Proposed ENERGY STAR heat-pump source | DEFERRED | Partner-submitted field rights remain unknown; owner prohibited signing, promotion, acquisition, publication, contact, and outreach in this work package | Owner explicitly reopens the source after appropriate human rights review authority is available |

## Superseded

| Item | Status | Superseded by | Evidence |
| --- | --- | --- | --- |
| Vercel as Data Foundry deployment target | SUPERSEDED | Cloudflare Workers | ADR-0005 and ADR-0006 |
